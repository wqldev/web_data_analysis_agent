/**
 * Write `data` to `path` so a reader never observes a half-written file and a
 * crash never leaves a truncated file at `path`. The bytes are written to a
 * uniquely-named temp sibling (so the final step is a same-directory `rename`,
 * which is atomic), flushed to disk with `fsync`, then renamed over the target.
 * On any error the temp file is removed and the original error is rethrown, so
 * `path` keeps its previous contents (or stays absent), never a torn mix.
 */
export declare function writeFileAtomic(path: string, data: string | Uint8Array): Promise<void>;
//# sourceMappingURL=atomic-write.d.ts.map